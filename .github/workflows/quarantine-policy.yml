name: Package quarantine policy

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  enforce:
    name: Enforce package quarantine
    if: github.repository == 'stella/tooling'
    permissions:
      contents: read
    uses: stella/.github/.github/workflows/quarantine-policy.yml@8cd71d93edeb4d7c433c6a75c4be409fbd1c50bb
