name: Package quarantine policy

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  enforce:
    name: Enforce package quarantine
    if: github.repository == 'stella/tooling'
    permissions:
      contents: read
    uses: stella/.github/.github/workflows/quarantine-policy.yml@76807b4423de63c1216a1ed7437268c4c1c20d7c
